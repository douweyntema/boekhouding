/var/log/dovecot*.log {
	weekly
	rotate 4
	compress
	delaycompress
	missingok
	notifempty
	sharedscripts
	create 0750 mailbox mailbox
	postrotate
		/bin/kill -USR1 `cat /var/run/dovecot/master.pid 2>/dev/null` 2> /dev/null || true
	endscript
}
