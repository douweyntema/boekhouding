all:

install:
	mkdir -p -m 755 $(DESTDIR)/etc/exim4
	install -m 644 src/exim4.conf $(DESTDIR)/etc/exim4
	mkdir -p -m 755 $(DESTDIR)$(PREFIX)/sbin
	install -m 755 config/update-exim4.conf $(DESTDIR)$(PREFIX)/sbin

clean:

.PHONY: all install clean
